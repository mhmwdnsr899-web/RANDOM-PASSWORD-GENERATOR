# RANDOM-PASSWORD-GENERATOR
It is application generate random passwords with ability to select from (letters,number..ect) and lenght of password.This app elso can save password in JSON file with (secert.key) by fernet .It is completely run offline to save password with ability to share it but use share key also to be able to read JSON file.There are versions [.exe,.apk, .ipa]
